# Use npm for Prisma CLI to avoid pnpm ESM issues
# This file helps npm resolve Prisma correctly

# Hoist Prisma packages so prisma generate can resolve them
public-hoist-pattern[]=@prisma/client
public-hoist-pattern[]=@prisma/engines

# Required for Prisma + pnpm monorepo
shamefully-hoist=true
